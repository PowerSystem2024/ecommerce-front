// Re-exportar tipos específicos para evitar duplicados
export type { ApiResponse, PaginationParams } from './api.types';
export type { 
  DashboardStats, 
  OrderSummary, 
  TopProduct, 
  SalesByCategory, 
  MonthlySales, 
  DashboardFilters 
} from './dashboard.types';

// Exportar tipos adicionales según sea necesario
